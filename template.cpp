#include <iostream>
#include <vector>
using namespace std;

// Most commonly used type aliases
using vi = vector<int>;
using vvi = vector<vector<int>>;
using ll = long long;
using pii = pair<int, int>;
using vb = vector<bool>;
using vs = vector<string>;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t = 1;
    // cin >> t;  // Uncomment for multiple test cases
    while(t--) {
        //solve();
    }
    return 0;
}